user: dean

core config:
  dbuser: root
  dbpass: 
  extra-php: |
    define( 'WP_DEBUG', true );
    define( 'WP_POST_REVISIONS', 50 );

core install:
  admin_user: dean
  admin_email: dverleger@apparatus.net

apache_modules:
  - mod_rewrite